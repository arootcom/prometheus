global:
  scrape_interval: 15s
  external_labels:
    datacenter: 'dc-east-1' # Важно! Помогает мастеру понять, откуда пришла метрика

scrape_configs:
  - job_name: 'local-apps'
    static_configs:
      - targets: ['localhost:9090']

